# Bug tracking

User-flagged bugs in the KrakenOS Open 3D inspector get a numbered record
here, fixed, pinned with a small targeted test, and folded into the
comprehensive penta-telescope regression validator.

## Workflow (per bug)

1. **Document** — add `NNNN-short-slug.md` (next free number, see the
   register below). Capture: symptom (the user's own words), the in-app
   repro bundle it came from, root cause, the fix (files + line refs), the
   test that pins it, and which validator phase now guards it.
2. **Fix** — make the change. Keep it minimal and root-cause, not a
   band-aid over the symptom.
3. **Test immediately** — write a small, specific test that fails before
   the fix and passes after. Prefer a display-free unit test against the
   narrowest seam (e.g. a `@staticmethod` on a real VTK actor) so it runs
   without an X server. **For a *visual* bug (selection color, ghost /
   leftover actors, handles — anything about what the user sees), a
   property-only test is NOT sufficient:** it must also render the scene to
   a PNG (off-screen under Xvfb) and check pixels (e.g. "negligible red,
   pink fill present"), and the fixer must open that PNG and visually confirm
   it. The all-red fix (0001) passed every vtkProperty assertion yet a
   second actor still painted a red block (0002) — only a rendered image
   caught it.
4. **Integrate** — add a phase to
   `KrakenOS/UI/validate_open3d_penta_telescope_comprehensive.py` so the
   bug is re-checked end-to-end on the real penta-prism cascade (load,
   cascade one by one, select / unselect, observe optical axis). Then
   regenerate the pre-push gate baseline:
   `python tools/penta_validator_gate.py --update-baseline`.

## Where reproductions come from

Users flag bugs with the in-app event recorder
(`KrakenOS/UI/services/open3d_event_recorder.py`). Bundles land under
`attachment/recorded_bug_repros/flag_<timestamp>/` (gitignored): a
`description.txt` (the complaint), a `screenshot.png`, and a `state.json`
snapshot. Analyze the newest by mtime; correlate with the matching
`recording_<timestamp>.json` event log.

## Test layers

- **Unit / display-free** — `KrakenOS/UI/validate_open3d_<bug>.py`, run via
  `.devenv/state/venv/bin/python -m KrakenOS.UI.validate_open3d_<bug>`.
- **Image-snapshot (visual bugs)** — `validate_open3d_<bug>_snapshot.py`
  renders the real scene to a PNG and counts pixels; it boots its own Xvfb
  when `DISPLAY` is unset. Inspect the PNG by eye, not just the counts.
- **Regression / end-to-end** — a numbered phase in
  `validate_open3d_penta_telescope_comprehensive.py`, gated on push by
  `.githooks/pre-push` -> `tools/penta_validator_gate.py` (boots Xvfb,
  blocks only on a PASS->FAIL flip vs `tools/penta_validator_baseline.json`).

## Register

| ID | Title | Status | Fix tests |
|----|-------|--------|-----------|
| [0001](0001-analytic-lens-selection-all-red.md) | Selecting an analytic lens renders solid red, no slide handle | Fixed | `validate_open3d_analytic_lens_select_not_all_red` + Phase 10 |
| [0002](0002-analytic-lens-selection-ghost-red-block.md) | Selected analytic lens leaves a "ghost red block"; slide-along-axis has no handle | Fixed (red block); slide-handle is a UX gap, not a bug | `validate_open3d_analytic_lens_selection_snapshot` + Phase 10 image check |
| [0003](0003-aspheric-achromat-many-faces-snapshot-key.md) | Aspheric achromat: "so many faces", `s` snapshot key dead in face editor, InvalidMeshWarning | Fixed (snapshot key + dialog-pixel capture, InvalidMeshWarning, all-red selection, 160-face display grouping, then root-cause OCC B-Rep import → 7 real faces) | `validate_open3d_step_promotion_mesh_warning_free`, `validate_open3d_optical_solid_face_grouping`, `validate_open3d_flag_dialog_capture`, `validate_open3d_brep_optical_solid_faces` (+ `_snapshot`) |
| [0004](0004-step-combined-move-rotate-gizmo.md) | "no slide handles" — combine rotate + translate into one Move/Rotate gizmo; arrows clear the arcs; free axial travel; live edge-gap readout | Fixed | `validate_step_rotation_handles`, `validate_open3d_step_translate_gap` + Phase 11 |
| [0005](0005-ghost-red-face-hover-edges.md) | "ghost red edges" — STEP face hover highlight painted red, reads edge-on as a red bar through the lens | Fixed (hover now uses shared gold accent) | `validate_open3d_step_face_hover_not_red` (+ `_snapshot`) + Phase 12 |
| [0006](0006-promoted-row-translate-arrows-shrink.md) | After promoting a STEP to an analytic lens, re-selecting it shrinks the big Move arrows to short grid stubs | Fixed (row gizmo sized off the body, shared length seam) | `validate_open3d_promoted_row_translate_arrow_length` (+ `_snapshot`) + Phase 13 |
| [0007](0007-thickness-dimension-on-optical-axis.md) | Thickness dimension label/arrow sits on the optical axis instead of offset to the side | Fixed (camera-aware offset into the screen plane, 8% margin like the 2D distance arrows) | `validate_open3d_thickness_dimension_offset` (+ `_snapshot`) + Phase 14 |
| [0008](0008-step-delete-clears-unselected-lens.md) | "lens element dissapear" — a stray Delete/BackSpace erased the imported lens with nothing selected | Fixed (delete-only label candidates drop the hardcoded "optical" fallback) | `validate_open3d_step_delete_requires_selection` (+ `_snapshot`) + Phase 15 |
| [0009](0009-thickness-overlay-skips-lens.md) | "thickness overlay skip the lens element" — persistent arrow measures straight through an imported lens; thicken both distances | Fixed (split row span around intervening overlays; refresh draws dimensions after the STEP bodies register; shared thicker line knobs) | `validate_open3d_thickness_overlay_skips_lens` (+ `_snapshot`) + Phase 16 |
| [0010](0010-ghost-yellow-face-highlights.md) | "ghost surfaces" — hover *edge* highlights stranded at the lens's old spot after Place→Center Row→Optical axis snaps the (aspheric) lens; re-light on hovering the now-empty region | Fixed (verified by user; resolved with the session's refresh-ordering/hover-clear changes + restart; recorder instrumentation kept as a tripwire) | recorder `stray_props_above_body` / `hover_outline_bounds` tripwire |
| [0011](0011-thickness-overlay-stale-after-move.md) | "Thickness overlay not auto-updated" — after a gizmo move the persistent `gap =` arrows keep the lens's old position; live readout is correct | Fixed (move commit does a full refresh when dimensions are shown, so they recompute at the new position) | `validate_open3d_thickness_overlay_live_update` + Phase 17 |
| [0012](0012-promoted-analytic-row-cant-slide.md) | "after changing to analytical lens, can't slide" — promoted optical-solid row fired a ~0.5 s retrace per drag step ("computes hard"); then handles lagged; then it reverted on release | Fixed (live cheap actor move of body+handles, defer the retrace to release; body centre tracks the live pose instead of the cached `center_world`) | `validate_open3d_promoted_row_slide`, `validate_open3d_saved_native_center_tracks_pose` + Phases 18, 19 |
| [0015](0015-terminal-element-rays-vanish.md) | "the ray still missing" — a beam splitter (any non-Image element) dropped on the **terminal** surface stripped its detector role, so the display filter silently dropped every traced ray | Fixed (final prescription surface is the terminal image plane regardless of optical type; physics/branching unchanged) | `validate_random_terminal_element_ray_display` + Phase 24 |
| [0016](0016-promoted-beam-splitter-cube-rays-vanish.md) | "the ray is still missing" — a promoted beam-splitter **cube** made every traced ray invisible (`ray_actor_count=0`); even all-Uncoated showed nothing | Fixed (promotion defaults faces to Uncoated not Absorber; display keeps every traced ray up to its terminal surface, only *un-folded* `escaped` gated by Show Clipped Rays — refined by 0018 so a deliberately folded reflect branch stays visible; analytic fit selects the flagged optical-axis pair so it doesn't over-count uncoated side walls) | `validate_open3d_traced_rays_always_visible` + Phase 25; `validate_open3d_promotion_auto_lens_faces`, `validate_open3d_promotion_analytic_fit` |
| [0017](0017-beam-splitter-cube-transmit-and-second-axis.md) | "ray transmitting stop right at the imaging lens entrance" + reflected branch had no optical axis | Fixed (an inferred straight-through cube exit no longer snaps the downstream Image plane onto the cube face in front of the lens; the traced-axis builder emits one axis per distinct fold direction so the reflected branch earns Optical Axis 2) | `validate_open3d_beam_splitter_transmit_and_second_axis` + Phase 26 |
| [0018](0018-reflected-branch-detector-plane-runaway.md) | "rays exit still bent" → then "where is the beam splitter 2nd path ray?" — reflected branch first rendered as a bent diagonal band (2D collapsed to a dot), then *vanished* after the first fix | Fixed twice: (1) the escaped-ray detector-plane projector force-landed grazing reflected rays ~6×10⁵ mm off-axis — now a ray must head within cos 80° of the plane normal before projecting, so the +X fold keeps its sane ~232 mm length; (2) that reclassified the fold `escaped`, which the 3D filter hides with Show Clipped Rays OFF — now an escaped ray that was folded by non-refractive steering (reflect/split/mirror/TIR) stays visible, so the 2nd path renders | `validate_open3d_reflected_branch_detector_bounds` (+ display-filter guards) + Phase 27 |
| [0044](0044-wavefront-3d-real-surface.md) | *Enhancement* — user: "can we just generate this wavefront directly in 3D? … Much better than this fake 3D from 2D" | Implemented (real z-buffered PyVista/VTK surface in a subprocess window via the **WFront 3D** button; 2D Zemax waterfall kept as the printout panel; no new dependency) | `validate_wavefront_3d_surface` + Phase 50 |
| [0045](0045-surface-shape-builder-nameerror.md) | Surface Shape Builder button crashed with `NameError: encode_custom_surface_value` (silent in UI — traceback only on console) | Fixed (workbench imports the symbol directly instead of relying on the editor's globals-sync, which only carried `decode_`; plus a `report_callback_exception` override so no UI action ever fails silently again) | `validate_surface_shape_builder_dialog_bindings` (display-free AST binding guard) |
| [0046](0046-cemented-doublet-thin-cement-body.md) | "3D lens element not matching 2D" / "duplicating itself" — a cemented doublet (`Doublet_4_surf_no_TDE.zmx`) models the bond as a real 7.5 µm `___BLANK` cement, so KrakenOS drew that microns-thick glass as a full-aperture standalone slab; 3D read as a third element while 2D collapsed it to a hairline (not a North-Star #2 violation — same data, a 3D legibility artifact) | Fixed in BOTH render paths (a sub-0.05 mm cement/bond layer keeps its actor + AAA optical surface but is drawn at opacity 0: hidden in `_iter_3d_side_body_meshes`, and kept hidden through the live `open3d_scene_refresh` rays-on opacity clamp that otherwise re-inflated it to a slab + extra rim; ray tracing untouched). A follow-up flag ("extra rim still there") traced to each lens row drawing BOTH a cap rim and a mechanical body-drum OD rim (5 rings, want 3) — round analytic bodies now draw their glassy fill but no duplicate OD ring, so the doublet reads as 3 rim lines | `validate_cemented_doublet_body_count` (display-free) + `validate_cemented_doublet_body_count_snapshot` (live rays-on render, asserts rim_count==3) + Phase 51 |
| [0047](0047-det-mode-blanks-reference-disks.md) | "Clicking Refs show only the Object disk, click Det, Object Disk vanish, not showing any Image Disk" — on the on-axis cemented doublet (no detector), toggling Det blanked the Object/Image reference disks | Det-blank fixed (suppression now gated on `Open3DSceneRefreshService._detector_coverage_will_draw` — a real detector target with active sensor dims AND `max_real_image_height > 0`, i.e. only when the coverage overlay actually replaces the disks; a strict subset, so detector phases 37–39 are unchanged). The "1 mm Image disk" half (on-axis Infinity focuses to a near-point) is a faithful read, left as an open design item (Finite-mode starter, or a real-detector footprint fallback) | `validate_det_coverage_gate` (display-free gate logic) + `validate_det_mode_keeps_reference_disks` (live Det-toggle render) + Phase 52 |
| [0048](0048-open3d-iso-orbit-clips-converging-cone.md) | "first view … second view, it starts clipping … 3rd view, clipped" — orbiting/zooming the Open-3D view **right after startup** sliced off the converging cone + image plane, but the same orbit was fine **after clicking a cardinal preset** (XZ/YZ/…) | Fixed (the "Iso" view's `set_camera_preset` else-branch used PERSPECTIVE — no `parallel_scale` — while every cardinal preset is PARALLEL; a perspective camera sits a finite distance away so an orbit swings the far geometry behind it into the clamped-positive near clip plane. Iso now fits an orthographic `parallel_scale` like the cardinal buttons; parallel renders behind-camera geometry and makes camera distance visually free. Backed by a parallel "keep the camera clear of the complete scene" dolly after refresh + on every interaction — both no-ops on the rendered image) | `validate_camera_iso_orbit_no_clip` (live: Iso is orthographic, orbit keeps all 8 scene corners in front, fixed frame draws the image-plane geometry the recorded perspective camera clips) + Phase 53 |
| [0049](0049-open3d-step-reselect-accumulates-gizmos.md) | "Camera selcted, but the Imaging Lens still get selected" / "LED STEP selected, previous two selection are not cleared" — clicking a different imported STEP solid left the previous solid's rotation gizmo on screen; three picks stacked three gizmos (recorded `rotation_handle_count` 6 → 12 → 18) | Fixed (`show_step_rotation_handler` was add-only — `ensure_for_label` returns early when a label has handles and adds six when it has none, but nothing removed the *prior* label's six. Now the inspector tracks a `_selected_step_labels` set and reconciles the live gizmos to it: a plain click collapses to `{label}` (tearing down the prior gizmo — the reported fix), Shift+click toggles a label in/out for intentional multi-select, and Ctrl is left as the camera-orbit modifier so it is not overloaded. New `Open3DStepRotationHandleService.remove_for_label`/`reconcile_to_labels` do the per-label teardown; the scene refresh re-applies the set so multi-select survives a rebuild) | `validate_open3d_step_reselect_single_gizmo` (display-free: selection-set logic + service reconcile never accumulates) + Phase 54 |
| [0050](0050-open3d-display-only-step-hover-ghost-after-move.md) | "the residul highlight at old location bug surface again" — sliding a display-only STEP (LED) with its gizmo left a gold face-hover edge outline stranded at the body's former pose (recorded `hover_outline_bounds` on the LED's old left face, `stray_props_above_body` firing); bug 0010 resurfacing for the labels its fix left pose-blind | Fixed (the face-metadata cache that the hover/pick read is pose-aware only for *analytic* labels; display-only labels — `camera`/`led`/`lens` — keep a pose-blind stat-only key to dodge the cold-load recompute, so after a move they handed back the body's former world coords. New `_invalidate_step_overlay_face_metadata_cache(label)` is called from the three pose setters every move routes through, so the next hover re-derives geometry at the new pose; lazy, so no move-time freeze. `_translate_step_overlay_actors` also drops the displayed hover outline on the live drag) | `validate_open3d_step_display_only_metadata_tracks_pose` (display-free: move via the public API, assert every face centroid tracks; negative control fails 7/7) + Phase 55 |
| [0051](0051-open3d-selected-step-muddy-orange-low-contrast.md) | "highlight edge color does not have contrast. And why this STEP color is orange? different from the rest" — a selected imported STEP body rendered as a flat, low-contrast orange shape | Fixed (`_set_step_actor_selected` turned on per-triangle edges in orange over a dense CAD tessellation — a wireframe that filled the body — with no fill change. Now it uses the app-wide selection idiom from `_set_row_actor_selected` (bugs/0001-0003): suppress the triangle edges and fill the body pink translucent `(1.0,0.45,0.65)`; the body's glass-edge rim actor keeps the silhouette) | `validate_open3d_step_selection_pink_snapshot` (image snapshot: selected adds body-sized pink fill absent unselected) + Phase 56 |
| [0052](0052-open3d-led-body-amber-inconsistent-palette.md) | "Why only the LED body still orange? and the highlighted edge is yellow, hard to see" — with nothing selected, the LED body rendered a saturated amber while camera/lens are grey-blue, and the gold hover edge was invisible on the warm body | Fixed (the LED overlay's *base* color was a deliberate amber `(0.95,0.62,0.16)` "light source" cue, duplicated in BOTH draw paths — `_step_overlay_display_spec` and `open3d_scene_refresh.refresh_scene`. Per user's call, de-ambered to the shared grey-blue glass `(0.30,0.36,0.46)` in both paths so all imported solids read as one palette and the gold hover regains contrast; reworded the now-inaccurate "orange LED"/"orange feature" Open-3D status strings. Legacy 3D renderer left amber+consistent. The "highlight offset from the edge" sub-point is logged as a display-only planar-clustering follow-up) | `validate_open3d_led_overlay_palette` (display-free: grey-blue, not amber, in both draw paths) + Phase 57 |
| [0053](0053-open3d-reanchorable-dimension-arrows.md) | FEATURE (motivated by "the object to LED distance is wrongly placed at the LED cables, should be at the LED bottom surface") — Ctrl-drag a thickness/distance dimension arrow's endpoint onto a surface/edge to re-anchor what it measures to | Added (measurement re-anchor; never moves an optical surface. `_dimension_anchor_overrides` per-row state persisted in layout settings; `apply_dimension_anchor_override` commits — object/LED row reuses `apply_led_object_edge_pick`, other rows store a measured-endpoint override drawn as a distinct magenta "S{i} measured = …" arrow. Ctrl+press on an arrow re-anchors via a new `_dimension_anchor_drag_state` in the mouse bindings; Ctrl on empty space still orbits). Revised on feedback: modal no-hold re-anchor + live real-arrow preview + snap highlight; editing a re-anchored value MOVES the Next element by editing the single upstream gap (`apply_reanchored_dimension_value`), QE suppressed | `validate_open3d_dimension_reanchor` (display-free: math + override stored, thickness unchanged, LED route, settings round-trip, modal contract, value-edit move) + Phase 58 |
| [0054](0054-open3d-object-led-dimension-value-moves-led.md) | "changing the thickness to 200 does not move the LED. (Instead it silently changed to 275)" — editing a re-anchored object→LED (`S0`) dimension value left the LED put, and the inline editor pre-filled the 275 mm object gap instead of the 212.6 mm measured distance (recording `flag_20260611_084621_151`) | Fixed (the LED is the imaged object — a STEP body, not an optical surface — so the bugs/0053 "move the Next element" path mapped its face to no row and refused. Now `apply_reanchored_dimension_value` detects row 0 + imported LED and routes to `_move_led_for_reanchored_value`, which shifts `led_object_edge_distance_mm` by the span delta to rigidly translate the LED so the measured face lands at the typed distance; no `rows[i].thickness` changes, refuses moving the LED behind the object plane. `edit_dimension` now pre-fills the measured `|ref_z − fixed_z|` for a re-anchored row) | `validate_open3d_dimension_reanchor` (display-free: LED-move + measured prefill) + Phase 59 |
| [0055](0055-open3d-click-on-plane-fov-solve.md) | FEATURE ("need to improve the Set Target FOV … do that graphically … Click on the object → pop up input box → input the FOV in horizontal dimension (not diameter) → two buttons: Solve for Thickness and Solve for Image/Sensor Size. The same … for Image plane") | Added (double-left-click the Object/Image plane → small box pre-filled with that plane's horizontal field width (4:3 width↔diagonal), two buttons: "Solve for Thickness" moves the object↔image conjugate pair so the field fills/maps to the sensor; "Solve for Image/Sensor Size" resizes `rows[-1].diameter` at the current `|m|`. New `QuickEstimationService.fov_solve` + `_conjugate_pair`/`apply_sensor_diagonal` + horizontal helpers; `_open_quick_estimation_fov_popup`/`_apply_quick_estimation_fov_solve`/`_maybe_open_fov_popup_from_double_click` on the inspector; `<Double-Button-1>` binding. Single click still just selects). **Follow-up (same session):** the Object plane was not pickable (only `pick_row_index` actors register for hover/double-click, and the reference planes draw circles, not surfaces) — a faint pickable `pv.Disc` is now placed at each plane via the QE overlay (`_pick_disk_actor`, gated on QE-enabled); and the Image popup gained a **Height** field next to Width — `fov_solve(..., width, height=None)` + `_sensor_wh`/`apply_sensor_rect` set `Ø = sqrt(W²+H²)` and store the rectangular detector dims (`height=None` keeps the prior 4:3 behaviour) | `validate_open3d_fov_plane_solve` (display-free: 4:3 mapping, all four solve cases, W×H image sensor, `sensor_active_dimensions` prefill, pick-disk registration, refusals, gesture + Height-field wiring) + Phase 60 (extended: live Object/Image pickability + W×H case) |
| [0056](0056-open3d-detector-fov-plane-not-pickable.md) | "still no hover highlight the FOV plane and can't double click select" (+ "it should be square in this case") — on the machine-vision layout the visible green `FOV 23.0×23.0` **square** is the *detector-coverage* overlay (a different service than the bugs/0055 QE pick-disk), drawn as a non-pickable line actor; with Det on the Object/Image clear-aperture disk is suppressed to opacity 0 (bugs/0047) and QE is off, so the Object plane had no pick geometry | Fixed (`DetectorCoverageOverlayService._pick_fill_actor` adds a faint opacity-0.08 filled **square** — kept square per the user — at BOTH planes with a `pick_row_index`: Object→row 0, Image→terminal `len(rows)-1`; independent of the QE/Refs toggles, untagged so the bugs/0047 disk-suppression + Phase 39 disk/label guards are unaffected) | `validate_detector_coverage` item 7 (display-free: pick-fill registers its row + both-plane wiring) + Phase 61 (live: Object row 0 & terminal Image row both pickable with Det on) |
| [0057](0057-open3d-object-fov-width-height.md) | FEATURE ("Please change the FOV input to Width x Height as well") — the Object-plane FOV popup had only a single horizontal "field width" entry (vertical extent always derived from the 4:3 aspect), unlike the Image popup which already took Width × Height | Added (object popup now shares the Width + Height layout, height optional; prefilled from new `QuickEstimationService.object_fov_dimensions()` = sensor rect ÷ \|m\|. `fov_solve("object", …)` routes W+H through `_sensor_wh`: height given → Ø=√(W²+H²) and sensor sizes to `\|m\|·W × \|m\|·H`; height blank → exact prior 4:3 behaviour, so old layouts are unchanged) | `validate_open3d_fov_plane_solve` (display-free: object W×H sensor + thickness solves, `object_fov_dimensions` prefill, both-popup Height-field wiring) + Phase 60 |
| [0058](0058-wire-attachment-cameras.md) | FEATURE ("There are a few cameras added in attachment/Cameras, can you wire them into camera selection option?") — two new camera bundles (STEP + datasheet) were on disk but absent from `CAMERA_DATABASE`, so the dropdown listed only hr25MCX | Added (two records spec'd from the datasheets, resolved attachment-relative via `_preferred_existing_path`: **Allied Vision shr661MCX12** — IMX661, 13392×9528, 46.2×32.87 mm Ø56.7, M72×0.75; **Japan Bopixel BC-GM65M12X4-F** — Gpixel GMAX3265 mono, 9344×7000, 29.90×22.40 mm Ø37.36, F-mount. Computed diagonals match the datasheets; no UI wiring change — the combobox already enumerates `camera_names()`) | `validate_attachment_paths` "every camera resolves attachment files" (display-free: step + datasheet under `attachment/` and exist, for all 3 cameras) |
| [0059](0059-open3d-toolbar-ray-count-sync.md) | "3D is missing Ray count, and it should sync with 2D one." — the only 3D ray-count entry was buried in the collapsible Live panel's Source section; the always-visible top View toolbar had Show rays but no count, so it read as missing vs the 2D "Ray fan count" | Added (a compact `CommitEntry` **Ray count** in `build_view_toolbar` next to Show/Pick rays, bound to `_editor_var("ray_count_var")` — the same shared `tk.StringVar` as 2D and the Live panel — committing via `_commit_live_control_update(sync_fields=True)`; not a defect, the sync mechanism already existed, this just surfaces it where Show rays lives. View row stays at 6/10 direct controls) | `validate_open3d_toolbar_layout` "Ray count synced" check + new `validate_open3d_ray_count_toolbar_sync` (display-free Tk: `editor_var` shares the var by identity, two-way mutation, fallback, toolbar source wiring); PNG-confirmed |
| [0061](0061-open3d-clipped-rays-sync.md) | "after enabling and disabling Miss ray, the miss ray still show up" + "3D 'clipped rays' has no toggle; it should sync with 2D both ways" — the 3D ray filter already read the editor's `show_clipped_rays_var` but had no UI control, so escaped/stray rays (an LED fan missing the lens) always drew; the "Miss" overlay toggle only gates the diagnostic markers, never the ray lines | Added (a **Clipped** Overlays checkbutton bound to the *shared* `show_clipped_rays_var` via `_editor_var` — the bugs/0059 pattern — so 3D and 2D flip the same `tk.BooleanVar`; new `_on_clipped_rays_changed` marks the 2D plot pending then refreshes the 3D scene. No filter-semantics change; "Miss"/terminal-diagnostics left as-is) | `validate_open3d_clipped_rays_sync` (display-free: menu wiring, handler marks-2D+refreshes-3D, shared-var identity, and a synthetic-path filter check — clipped OFF hides only the escaped-non-folded stray) + Phase 63 |
| [0060](0060-open3d-variable-thickness-solve.md) | FEATURE (Phase 1 of "most of the 2D functions in 3D") — "Can make thickness or multiple thicknesses set to Variable and solve for Best Focus or Best Collimate?" The embedded Open 3D inspector had no way to mark a gap Variable or run the 2D best-image solve, and no Best Collimation objective existed in either view | Added (a **Solve (Variable thickness)** panel section: a checkbox per thickness gap toggling the *shared* `SurfaceRow.optimize_thickness` flag — so a gap flagged in 3D is Variable in 2D too — plus **Solve Best Focus** / **Solve Best Collimation** buttons. New `Open3DSolveService` runs the editor's 1-D minimiser per Variable gap (coordinate descent for several). Best Focus reuses the existing spot-RMS solver; net-new **Best Collimation** minimises the closed-form paraxial output vergence `\|1/s'\|` from the object-independent system ABCD — smooth, deterministic, zero exactly at collimation (object lands at the front focal distance EFL−ppa), chosen over a real-ray angular spread that vignetted to noise. Object gap is collimation-only; the terminal Image gap is never a target. The run hook captures history + retraces like Snap to FOV) | `validate_open3d_thickness_solve` (display-free: source contracts + collimation V-shape/zero-vergence/front-focal + service gap-mutation across all five machine-vision layouts + Best Focus delegation on the fast layout) + Phase 62; PNG-confirmed |
| [0062](0062-open3d-clipped-vignetting-parity.md) | "disable clipped rays still show up" — follow-on to 0061: that synced the toggle *state*, but 2D and 3D ran *different filters* under it. 2D clipped-OFF kept only `hit_detector`; 3D hid only escaped-non-folded, so vignetted `stopped`/`missed_detector` strays (the LED fan past the Ø19.36 aperture) still drew in 3D | Fixed (3D filter only — tightened `ray_path_visible_without_clipping_from_events` to the 2D rule: visible-when-OFF iff `hit_detector` OR a deliberate fold. Folds still survive — bugs/0016 mirror scene, bugs/0018 beam-splitter 2nd path — every non-folded stop/miss/absorb/escape is now gated like 2D; bug-0022 don't-blank fallback intact. 2D filter untouched) | new `validate_open3d_clipped_vignetting_parity` (display-free: synthetic predicate + real fold-free datasheet trace where 3D clipped-OFF count == 2D detector-hit count) + Phase 64; retargeted `validate_open3d_traced_rays_always_visible` (Phase 25: non-folded stop/miss/absorb now hidden, folded-survives cases added) + `validate_open3d_clipped_rays_sync` check F expectation tightened to {hit, folded-escape} |
| [0063](0063-open3d-canvas-pick-selected-element-buttons.md) | "when I click the STEP element in the right panel browser, the 'Selected Element' buttons enabled. However, direct clicking on the element in the 3D canvas, those buttons remains grayed out." — the button-enable logic (`_update_properties`) keys off the browser tree IID; the browser click sets it but the 3D-canvas pick selected the body (gizmo/highlight) without ever syncing the admin panel | Fixed (canvas pick now mirrors its selection into the browser. New `Open3DStepAdminPanel.select_from_canvas(iid)` sets `_selected_item_id`, selects the matching tree row, and runs `_update_properties`; `Kraken3DInspector.sync_step_admin_canvas_selection` + `_open3d_browser_iid_for_table_row` map the pick to its IID; `_on_left_button_press` calls it for both the STEP-overlay pick (`overlay:<label>`) and the row pick. Both entry points now land on the same IID → same buttons. Supporting refactor: extracted `_selection_flags_for_iid` + `_compute_selection_button_states` as the single source of truth; property-var writes hardened to `.get()` so the panel runs headless) | new `validate_open3d_canvas_pick_enables_buttons` (display-free: source contracts that both picks sync + select_from_canvas updates; behavior parity — canvas-pick IID lights the same buttons as the browser, empty selection grays all) + Phase 65 |
| [0064](0064-open3d-drag-to-resize-imported-solid.md) | FEATURE ("Cube Beam Splitter 50×50×50 → 55×55×78; click a face, drag to grow the dimension, confirm, direct-edit; two right-angle prisms must grow together; then assign surfaces + glue onto the LED") — no way to resize an imported STEP solid existed | **Largely landed** (geometry kernel + overlay/promotion wiring + right-click **Resize Solid…** popup + interior 45° coating recovered as a selectable face + the pure drag-a-face **gesture planner**, Phases 67-70, + BRANCH_README/Sphinx docs; only the live drag-arrow VTK widget — unverifiable headless — remains as an in-app step). Grounded on the real vendor part (2 prisms, 50³, coating normal (1,1,0)/√2): the 45° coating stays valid only when the two diagonal-spanning axes scale together, so a detected splitter resizes with 2 DOF (square cross-section + free depth) — which also couples the prisms. Resize runs in **mesh space** (a non-uniform `BRepBuilderAPI_GTransform` degrades analytic Plane faces to BSpline); detection reads the original B-rep planes. Promotion + face metadata inherit the resize automatically (it meshes the transformed overlay) | `validate_open3d_solid_resize` (kernel, 15 checks incl. 3 vs the real vendor STEP) + `validate_open3d_solid_resize_overlay` (wiring + UI source contracts, 17 checks) + `validate_open3d_beam_splitter_coating_recovered` (coating face, 11 checks) + `validate_open3d_resize_gesture` (gesture planner, 24 checks incl. a geometry-integration check that a coupled drag keeps the coating at 45°); Phases 67-70 |
| [0065](0065-open3d-offbeam-promoted-solid-display-only.md) | "randomly put beam splitter off the ray path … the rays heir wired … rays seems to chase the beam splitter, behave like sequential rather than non-sequential … Image circle shown offset" (recording `flag_20260612_073237_607`) — a promoted beam-splitter cube dragged clear of the beam corrupted the on-axis trace (focus short of the detector, diverging, image circle offset) even with `use_nonseq=true` | Fixed (root-cause, non-sequential — NOT a sequential dodge; the reverted `02323d2`/`01a7743` forced sequential and made it a vignetting surface). The solid's lateral `desp_x`/`desp_y` was copied onto `surface.DespX`/`DespY` as a propagating coordinate break in `_build_system_from_specs` — the single chokepoint every paraxial/best-focus/spot-RMS/image-circle/live-trace build funnels through — so the off-axis cube leaked into the centered prescription. New `offbeam_optical_solid.neutralize_offbeam_inert_solids`, called at the top of `_build_system_from_specs`, replaces each **off-beam inert** promoted solid with a flat zero-power AIR surface (same thickness/diameter/kind → surface count + axial chain preserved; desp/tilt zeroed, `Solid_3d_stl`→None, promotion/face/output-port metadata dropped) so it contributes **zero** optical effect — a **display-only** scene body (the inspector draws it from the row overlay, not these build specs; cf. bugs/0021). A coated splitter or an on-/near-beam solid stays in the trace; an intentional decentered mirror is untouched. Off-beam = nearest transverse edge clears the beam radius ≥2× (conservative) | `validate_open3d_offbeam_solid_display_only` (display-free, 18 checks: classifier + neutralized-spec + real `_build_system_from_specs` prescription — killer check: off-beam-cube prescription == plain-AIR-spacer, zero optical effect; verified failing before the fix) + Phase 66 |
| [0066](0066-open3d-coated-splitter-snaps-to-axis.md) | "After assigning partial reflecting surface and closed the Face Editor" — a beam-splitter cube parked off the beam, given a Beam Splitter coating, **snapped back onto the optical axis** (with disconnected thickness overlays) instead of staying put and in the non-sequential trace | Fixed (schema mismatch). `solid_has_active_coating` read `OpticalSolidFaces` as a bare `list`, but the persisted schema is a **dict** `{"version", "faces": [...], …}`, so every real coated solid read as UNCOATED → it became eligible for the bugs/0065 off-beam neutralization, which dropped the splitter from the trace and zeroed its decenter (body snapped on-axis; the stray overlays were the dimension service measuring to the wrong body). Now reads through `normalize_optical_solid_face_metadata` (handles both the dict schema and the legacy bare list). bugs/0065's guard missed this because its fixtures used a bare list | `validate_open3d_coated_solid_schema_exempt` (display-free: dict-schema + bare-list coating detection, killer — a dict-schema splitter pushed far off-beam is **not** neutralized and keeps its `DespX` coordinate break; real machine-vision row 6 reads coated) + a dict-schema check (`A2b`) added to `validate_open3d_offbeam_solid_display_only` + Phase 71 |
| [0067](0067-open3d-offbeam-uncoated-solid-body-snaps-to-axis.md) | "The beam splitter snap to optical axis as soon as it is coverted to optical row and face editor launched" (recordings `flag_20260612_102753_518` / `_103030_081`) — while still **uncoated**, a promoted solid parked off the beam had its 3-D body snap onto the optical axis at promotion / Face-Editor launch (row-6 actor bounds centered on X=0); only after a coating was assigned did it return off-axis | Fixed (display-only — the optical solve is untouched). bugs/0065 correctly drops an off-beam **inert** solid from the trace (zeroing `SDT.DespX/DespY`, leaving `EEE[index]` a placeholder), but in `three_d_scene_tools._iter_3d_optical_surface_meshes` the body geometry comes from the live row STL while its placement comes from the *neutralized* on-axis `TRANS_2A[index]` — so `_stl_mesh_with_world_transform` planted the live geometry on the axis. New pure `offbeam_optical_solid.offbeam_neutralized_body_transform` restores the lateral station (`translation += R @ desp`, **exact** for an untilted solid since the two builds' `TRANS_2A` differ only by `R @ desp`); fires only on the neutralization signature (promoted + live decenter yet built `DespX ≈ 0`), so a coated splitter (`DespX ≠ 0`) no-ops. Wired in before the body is placed; trace + bugs/0065 focus fix untouched | `validate_open3d_offbeam_body_stays_offaxis` (display-free, 16 checks: pure helper contract + real `_build_system_from_specs` round-trip — killer: re-decentered transform == non-neutralized `TRANS_2A[cube]` station `[−55,0,110]`, and a no-op stub fails it; coated-splitter no-op; wiring present) + Phase 72 |
| [0068](0068-open3d-camera-step-renders-thin-window.md) | "Camera STEP is wrong." (recording `flag_20260612_113305_426`, layout `machine_vision_120mm_65M`) — the 65 MP vendor camera (Japan Bopixel BC-GM(C)65M12X4-F) drew as a thin ~6 mm sensor-window slab floating at the sensor instead of its real 80×80×96 mm body (cf. `attachment/65M.png`) | Fixed (display-only). The camera overlay loaded with `largest_component=True`, and `_largest_connected_step_component` keeps the single connected region with the **most triangles** (`argmax` over per-region cell counts). On this 37-solid assembly the densest part is a small, highly-curved sensor-window cover (solid #3, 41.75×35.75×6.24 mm, ~50k tris @0.1 mm) while the real body (solids #0/#19, 80×80×29 mm, ~23× the volume) is a simpler box with far fewer triangles and loses the vote — so the camera collapsed to its window. A camera is a multi-part assembly (body + heat sink + mount + connectors), so the overlay now renders the **whole** thing (`largest_component=False`) in both the build (`_transformed_imported_camera_step_mesh`) and the cache warm-up (`_open3d_step_cache_warmup_specs`). The full assembly is only ~116k tris @0.1 mm — barely more than the slab alone — inside the memoised "100k cells render comfortably" envelope. The lens overlay's `largest_component` (isolating one glass element) is untouched | new `validate_open3d_camera_step_full_body` (display-free + portable: synthetic-mesh proof that the real selector keeps the dense-but-tiny region not the big body; behavioral wiring that both camera codepaths request the full assembly — fails before / passes after; skip-if-absent real-cache enrichment that the largest-component cache IS the 41.8×35.8×6.2 mm slab) + Phase 73; live render confirmed in-app |
| [0069](0069-open3d-fov-sensor-rect-transposed.md) | "the FOV numerical value is correct, but the rectangle drawn is reverse" / "both object and image WxH seems reverse" (`attachment/3D.png`, sensor 29.9×22.4 mm) — the numeric label read right but BOTH the green object-FOV rect and the orange image-sensor rect drew **portrait**: the long 29.9 mm width ran along the VERTICAL screen axis | Fixed (display-only — the optical solve is untouched). All three in-plane rectangle producers fed the sensor **width** to the vertical axis: the shared `scene_target_active_footprint_polylines` (orange sensor rect + the 2D layout footprint) mapped width→`tangent` (+Y vertical); `detector_coverage_overlay_specs` (green object-FOV rect) and `quick_estimation_overlay` (yellow recommended-sensor rect) both built `_rect_points(pt, u, v, …)` where `_basis` returns `u=+Y` (vertical) — so width landed vertical. Now width spans the HORIZONTAL in-plane axis: footprint offsets width by `bitangent * half_w` / height by `tangent * half_h`; both overlay rects build `_rect_points(pt, v, u, …)`. The footprint producer is shared with the 2D layout, so 2D is corrected too; `validate_layout_plot_controller` had an assertion locking in the old transposition (width→Y) and was updated to landscape (width→X) | new `validate_open3d_fov_rect_orientation` (display-free + portable, pure geometry: on a non-square landscape sensor the shared footprint and the object-FOV rect put width on horizontal X / height on vertical Y, X>Y; source-wiring fail-before/pass-after that footprint maps width→`bitangent` and no longer maps height→`bitangent`, and both overlay rects build `_rect_points(…, v, u, …)`) + Phase 74; user confirms landscape in-app |
| [0070](0070-open3d-bopixel-65m-m42-mount.md) | FEATURE — "The Bopixel camera 65MP, can change to M42? … I am using M42 version, not F-mount version" + "The distance between the edge to the camera sensor is 11.5mm". The 65 MP Japan Bopixel BC-GM65M12X4 database entry was the F-mount variant (F Mount, 92 mm body, 46.5 mm flange, F-mount STEP) | Changed (data). The F-mount entry in `camera_database.py` was **replaced** (not duplicated) with the M42 variant: `lens_mount` `M42 Mount`, `camera_front_to_sensor_mm` **11.5** (was 46.5 — this drives `camera_front_z = image_z − front_to_sensor`, seating the body 35 mm closer), `body_dimensions_lwh_mm` (66.3, 80.6, 80.0) measured from the vendor STEP (was 92×80×80), and the M42 STEP path. The 29.9×22.4 mm / 65 MP sensor (Ø37.36 image circle) is unchanged — only the lens-mount hardware/flange differs. The gitignored layout `machine_vision_120mm_65M.py` points `camera_model`/`camera_step_path` at the M42 variant | new `validate_open3d_bopixel_m42_camera` (display-free: A — the tracked DB always-runs checks that the M42 entry has M42 Mount / 11.5 mm flange / the unchanged landscape sensor / the M42 STEP + body, the old F key is gone, and no 65M entry is still F-mount; B — skip-if-absent that the gitignored layout references the M42 camera+STEP) + Phase 75 |
| [0071](0071-open3d-fov-popup-one-box.md) | FEATURE — "Double click the FOV input box: can user … just enter value in one of the box, the other value should be auto calculated (or leave blank and calculate after pressing either of the buttons)". The popup's Width + Height boxes **required a positive Width** (bugs/0057 derived a blank Height at 4:3, but a blank Width was rejected) | Changed (display-only — the optical solve is untouched). `_sensor_wh(width, height, aspect=None)` now lets **either** side be omitted (blank → derive it from `aspect`, the live sensor's W:H, default 4:3); a value that *is* typed but non-positive/non-finite is still rejected. `fov_solve` gained an `aspect` param threaded into both branches. The popup grew a grey hint, a `_read_dim` per-box parser (blank → derive, bad → refuse with a status note), and a `run()` that errors only when **both** boxes are blank and computes the live aspect from the prefilled dims, forwarding it through `_apply_quick_estimation_fov_solve` → `fov_solve`. The width-only path + `horizontal↔diagonal` mapping are unchanged (bugs/0055/0057 still hold) | extended `validate_open3d_fov_plane_solve` (new `_test_solve_single_dimension`: Height-only → derive Width at 4:3 (16×12/Ø20); Height-only object thickness == the width-only conjugate solve; a custom 29.9:22.4 aspect fills the blank box; both-blank refused; `_read_dim` + `aspect` wiring) + **Phase 60** extended with the live one-box mirror (no new phase) |
